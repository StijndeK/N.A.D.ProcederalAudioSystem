using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class PDynamicCourse
{
    // aspects can change: live on live constant changing parameters, per smaller cycle of certain length (meausre for exmaple), per cycle (when environment completely changes)

    // create course bevorehand or to chooes from
    public static List<int> CreateCourse()
    {
        return new List<int>();
    }
}
